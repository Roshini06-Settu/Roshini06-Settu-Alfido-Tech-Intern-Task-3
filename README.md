# System Utility – Simple Command Runner

## 📌 Objective
To build a small system utility that demonstrates process creation
and command execution using C++.

## 🛠 Description
This project implements a simple shell-like command runner.
The program accepts user commands, executes them using the system,
handles errors gracefully, and exits cleanly when the user types `exit`.

## ✨ Features
- Executes system commands
- Graceful error handling for invalid commands
- Continuous input until exit command
- Simple and user-friendly interface

## 💻 Technologies Used
- Language: C++
- Compiler: g++ (MinGW)
- Operating System: Windows

## ▶️ Compile and Run Instructions

### Compile
```bash
g++ src/main.cpp -o main.exe
